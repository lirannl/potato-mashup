{
  "name": "liranmashup",
  "version": "1.0.0",
  "description": "A mashup application",
  "main": "index.ts",
  "scripts": {
    "run-prod": "node index.js",
    "start": "npx ts-node src/index.ts",
    "debug": "export $(cat dev.env | sed 's/#.*//g' | xargs) && npx ts-node src/index.ts",
    "build": "npx tsc && docker build -t lirannl/potato-mashup ."
  },
  "author": "Liran Piade",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.0.27",
    "typescript": "^3.9.7"
  },
  "devDependencies": {
    "ts-node": "^8.10.2"
  }
}
